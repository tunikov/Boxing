import { initialPositions as IP } from '../constants'

export default {
  leftBoxerHealth: 100,
  rightBoxerHealth: 100,
  leftBoxerPosition: IP.leftBoxer,
  rightBoxerPosition: IP.rightBoxer,
  winner: null,
}
